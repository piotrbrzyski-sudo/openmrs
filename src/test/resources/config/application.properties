# Defaults target the controlled Docker Compose environment. Every value can be overridden
# by the environment variables documented in README.md.
ui.base-url=http://localhost:8080/openmrs
api.base-url=http://localhost:8080/openmrs/ws/rest/v1
username=admin
password=Admin123
login.location=Inpatient Ward
visit.type=Facility Visit

browser=chrome
headless=false
timeout.seconds=40
cleanup.created-patients=true
